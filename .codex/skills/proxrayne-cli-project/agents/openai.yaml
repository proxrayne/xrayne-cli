interface:
  display_name: "Proxrayne CLI Project"
  short_description: "Project rules for the standalone xrayne-cli repository"
  default_prompt: "Use $proxrayne-cli-project before changing the standalone xrayne-cli repository."
